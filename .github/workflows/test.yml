---
name: Test

on:
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_call:
  workflow_dispatch:

jobs:
  test:
    uses: latamgateway/ruby-linters-latam/blob/main/.github/workflows/rspec.yml@cb3d2bd
